// import { get_wishlist_ids } from '@utils/get_wishlist_ids.util.js';

export const set_up_wishlist = () => {
  // const wishlistIds = get_wishlist_ids();

  // document.querySelector('.button-favourite').addEventListener('click', () => {
  //   window.location.replace('/wishlist.html');
  // });

  // const wishlistButtonNode = document.querySelector('.button-favourite');

  // !!wishlistIds.length && wishlistButtonNode.classList.add('with-label');
  // wishlistButtonNode.setAttribute('data-label', wishlistIds?.length || 0 );
};
